import React, { Component } from 'react'
import { Text, View } from 'react-native'
import strings from '../../constants/lang'

export default class Login extends Component {
    render() {
        return (
            <View>
                <Text>{strings.DUMMY_TXT}</Text>
            </View>
        )
    }
}
